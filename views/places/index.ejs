<% layout('layout/app') %>

<h1>All Places</h1>
<a href="/places/create">Add new place</a>
<ul>
  <% places.forEach(place => { %>
  <li><a href="/places/<%= place._id %>"><%= place.title %></a></li>
  <% }) %>
</ul>
